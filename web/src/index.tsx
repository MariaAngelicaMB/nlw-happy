import React from 'react';
import ReactDOM from 'react-dom'; // integracao de react com DOM

import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

